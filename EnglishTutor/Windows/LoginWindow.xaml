<Window x:Class="EnglishTutor.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EnglishTutor — Вход"
        WindowState="Maximized" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <!-- Centered login card -->
        <Border Width="480" MaxHeight="620"
                CornerRadius="16" Background="White"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" Color="#000000" Opacity="0.12" ShadowDepth="0"/>
            </Border.Effect>

            <StackPanel>
                <!-- Blue header banner -->
                <Border Background="#3B6FE8" CornerRadius="16,16,0,0" Padding="24,32">
                    <StackPanel>
                        <TextBlock Text="📚" FontSize="56"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock Text="EnglishTutor"
                                   FontFamily="Lora" FontSize="32" FontWeight="Bold"
                                   Foreground="White" HorizontalAlignment="Center"
                                   Margin="0,0,0,6"/>
                        <TextBlock Text="Интерактивный учебник английского языка"
                                   FontFamily="Nunito" FontSize="14"
                                   Foreground="White" Opacity="0.8"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Form area -->
                <StackPanel Margin="40,32,40,40">
                    <TextBlock Text="Добро пожаловать!"
                               FontFamily="Lora" FontSize="24"
                               Foreground="#1A1D3A" Margin="0,0,0,6"/>
                    <TextBlock Text="Войдите в аккаунт или зарегистрируйтесь"
                               FontFamily="Nunito" FontSize="13"
                               Foreground="#6B7280" Margin="0,0,0,28"/>

                    <!-- Username -->
                    <TextBlock Text="Логин"
                               FontFamily="Nunito" FontSize="13" FontWeight="SemiBold"
                               Foreground="#374151" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtUsername"
                             Style="{StaticResource ModernTextBox}"
                             Height="48" FontSize="15" Margin="0,0,0,16"/>

                    <!-- Password -->
                    <TextBlock Text="Пароль"
                               FontFamily="Nunito" FontSize="13" FontWeight="SemiBold"
                               Foreground="#374151" Margin="0,0,0,6"/>
                    <PasswordBox x:Name="PbPassword"
                                 Style="{StaticResource ModernPasswordBox}"
                                 Height="48" FontSize="15" Margin="0,0,0,8"/>

                    <!-- Error / success message -->
                    <TextBlock x:Name="TxtError"
                               FontFamily="Nunito" FontSize="13"
                               TextWrapping="Wrap"
                               Visibility="Collapsed" Margin="0,0,0,4"/>

                    <!-- Login button -->
                    <Button x:Name="BtnLogin" Content="Войти"
                            Style="{StaticResource PrimaryButton}"
                            Height="52" FontSize="17"
                            Margin="0,16,0,0"
                            Click="BtnLogin_Click"/>

                    <Separator Margin="0,20" Background="#E2E8F0"/>

                    <!-- Register prompt -->
                    <TextBlock Text="Нет аккаунта?"
                               FontFamily="Nunito" FontSize="13"
                               Foreground="#6B7280"
                               HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <Button x:Name="BtnRegister" Content="Создать аккаунт"
                            Style="{StaticResource SecondaryButton}"
                            Height="48" FontSize="15"
                            Click="BtnRegister_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
